/*! Voxel Tunnel by lsdlive | License: CC BY-NC-SA 3.0 Unported | https://www.shadertoy.com/view/MscBRs */
precision highp float;uniform float uTime;uniform vec2 uResolution;
#define time uTime/1.75
mat2 r2d(float a){float c=cos(a),s=sin(a);return mat2(c,s,-s,c);}vec2 path(float t){float a=sin(t*.2+1.5),b=sin(t*.2);return vec2(2.*a,a*b);}float g=.0;float de(vec3 p){p.xy-=path(p.z);float d=-length(p.xy)+4.;p.xy+=vec2(cos(p.z+time)*sin(time),cos(p.z+time));p.z-=6.+time*6.;d=min(d,dot(p,normalize(sign(p)))-1.);g+=.015/(.01+d*d);return d;}void main(){vec2 uv=gl_FragCoord.xy/uResolution.xy-.5;uv.x*=uResolution.x/uResolution.y;float dt=time*6.;vec3 ro=vec3(0,0,-5.+dt);vec3 ta=vec3(0,0,dt);ro.xy+=path(ro.z);ta.xy+=path(ta.z);vec3 fwd=normalize(ta-ro);vec3 right=cross(fwd,vec3(0,1,0));vec3 up=cross(right,fwd);vec3 rd=normalize(fwd+uv.x*right+uv.y*up);rd.xy*=r2d(sin(-ro.x/3.14)*.3);vec3 p=floor(ro)+.5;vec3 mask;vec3 drd=1./abs(rd);rd=sign(rd);vec3 side=drd*(rd*(p-ro)+.5);float t=.0,ri=.0;for(float i=.0;i<1.;i+=.01){ri=i;if(de(p)<.0)break;mask=step(side,side.yzx)*step(side,side.zxy);side+=drd*mask;p+=rd*mask;}t=length(p-ro);vec3 c=vec3(1)*length(mask*vec3(1.,.5,.75));c=mix(vec3(.2,.2,.7),vec3(.2,.1,.2),c);c+=g*.4;c.r+=sin(time)*.2+sin(p.z*.5-time*6.);c=mix(c,vec3(.2,.1,.2),1.-exp(-.001*t*t));gl_FragColor=vec4(c,1.);}